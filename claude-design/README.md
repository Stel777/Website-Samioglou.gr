# Claude Design Prototype

This folder contains the prototype exported from Claude Design.

Original archive kept at the repo root:

- `Website Samioglou-claude design.zip`

The root of the repository still contains the earlier Codex static prototype and the scraped/inspiration source files.

Suggested next step for implementation:

1. Review `index.html`, `styles.css`, and `src/`.
2. Decide whether to promote this prototype to the repo root or convert it into a proper app structure.
3. Preserve the source material in:
   - `samioglou_scraped_MD.md`
   - `content_inventory/`
   - `inspiration/`

